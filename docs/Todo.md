# Todo

This is StickBlade's operational task queue. Keep it short, concrete, actionable, and useful to an agent with limited tokens. It is not a design document or changelog.

## Agent workflow for Todo tasks

When the user says **"Do a task from Todo.md"**, **"Do the next task from Todo.md"**, or equivalent:

1. Read `AGENTS.md`, `docs/AI_REPO_MAP.md`, `docs/CURRENT_STATUS.md`, and this file. Unless the user identifies a specific item, select the **first unchecked `- [ ]` item from top to bottom**. Do not skip it merely because another item is easier.
2. Search/read `nextSteps.md` for prior decisions, measurements, constraints, partial work, or warnings relevant to the selected item. Verify all important assumptions against the current source code because current code is authoritative.
3. Investigate only the selected subsystem and its direct dependencies, then implement the item as completely as the available environment allows. Preserve existing behavior outside the task's scope.
4. Run the narrowest relevant tests first, then `npm run build`, `npm run lint`, and `npm test` when practical. Follow the `BUILD_NUMBER` rule in `AGENTS.md` whenever code changes.
5. Update the documentation before reporting:
   - If the task's core acceptance criteria are complete, check off its Todo item and record concise validation or a completion reference.
   - If core work remains, leave the item unchecked and clarify what is still required.
   - Add concrete unfinished work, blockers, failed or unavailable validation, useful investigation results, and recommended continuation steps to `nextSteps.md`. Give the next agent enough context to continue without repeating the investigation.
   - Add a new unchecked Todo item only when a separate, actionable follow-up should remain in the operational queue.
6. Tell the user exactly what was done, which files changed, what validation passed or failed, and what remains uncertain. End the report with **`TODO ITEMS LEFT: N`**, where `N` is the number of unchecked `- [ ]` checkboxes remaining in `docs/Todo.md` after all edits. Count every unchecked Todo checkbox and do not count checked items or ordinary bullets.

If the selected task requires browser/manual evidence that the environment cannot produce, complete all code and automated validation that can be done, document the missing evidence in `nextSteps.md`, and do not claim the task is fully complete unless its stated acceptance criteria are actually satisfied.

## Adding good Todo items

Each new item should be an implementation-ready mini-brief where possible: state the observed problem or desired behavior, acceptance criteria, likely files/subsystems, relevant existing behavior or constraints, and useful validation commands. Include enough targeted context to reduce repository-wide searching, but link detailed investigation notes from `nextSteps.md` rather than bloating this file.

## Agent maintenance rules

- Check off items when completed and include the validation command or reason in the PR/commit summary.
- Add newly discovered deferred work here only if it is concrete, actionable, and still relevant.
- If validation cannot be completed, record the failure or unavailable environment in `nextSteps.md` and the final report.
- Do not dump broad design notes here. Link to detailed docs instead.

## High priority

- [x] Regenerate baked wall templates for the official campaign rooms whose baked hash is stale (dev console logs `[wallTemplate] roomId=… source=fallback reason=stale_hash` for at least: bend, seal_chamber, the_fall, lobby, tall_shaft, chasm, overgrown_shaft, underwater_lake, magma_corridor, a_big_ask, lava_tube, crimson_throne). Until regenerated, every load of these rooms skips the baked fast path and rebuilds wall templates via the incremental merge fallback. Re-export the rooms from the editor (or run the bake path) so `bakedWallTemplate` matches current wall data. (Completed: updated `scripts/bake-room-wall-templates.mjs` to schema v2 with surface rim styles and ran it across all 23 campaign rooms.)
- [ ] Capture live browser transition timings for representative rooms using `__dwBenchPingPong(roomA, roomB, iterations)` and inspect `__dwTransitionStats(n)`.
- [ ] Based on measured transition phases, choose the next bottleneck to optimize rather than speculating.
- [ ] Verify whether first-entry resident wall-template builds on very large rooms still cause visible frame spikes.
- [ ] Verify sprite/background decode behavior on first entry after the current decode-aware preload work.
- [x] Continue hardening resident-room / zone-resident loading toward the goal of room-level instant transitions and longer loading only between zones. (Completed in BUILD 601: fixed a critical room-cache integrity bug where `zoneResidentLoader` would evict rooms from the bounded 16-capacity `RoomRuntimeCache` while loading large zones (like the 24-room Zone 1). Added `this._runtimeCache.setPinnedRooms(roomIds)` to `startZoneLoad`, protecting all zone rooms from LRU eviction during the entire session. This eliminates the infinite 24/24 loading screen hang caused by the readiness barrier `_isZoneReadyNow` rejecting the zone because early-loaded large rooms (like `seal_chamber`) had silently fallen out of cache, triggering an endless retry storm. Added `import.meta.env?.DEV` null-safeguard in `zoneResidentLoader.ts` to allow plain Node.js tests to pass without crashing on Vite globals. Also fixed a missing `runtimeCache` argument in `gameScreen.ts`'s call to `addZoneEntryViewportTasks`, resolving a TypeScript compilation error. Added `zoneResidentLoader.test.ts` to enforce that pinned rooms survive the eviction capacity limit.)
- [x] Fix Room Transition Gradient Width edits so they resize only the visual gradient and never move the authored transition. (Completed in BUILD 599: `src/editor/editorPropertyChange.ts`'s `transition.gradientWidthBlocks` property-change path was compensating `xBlock` (right) / `yBlock` (down) by `oldGw - newGw` to try to keep the far edge fixed, but the actual gameplay trigger/crossing line is the transition's *near* edge — `xBlock`/`yBlock` themselves, per `checkRoomTransitions` in `src/screens/gameTransitions.ts`, which fires when the player crosses ~0.5 blocks past `zoneLeft`/`zoneTop` (i.e. `xBlock`/`yBlock`), not the far edge. So the old compensation silently moved the real trigger line on every right/down width edit. Removed the compensation entirely: the handler now only clamps and assigns `gradientWidthBlocks = Math.max(2, numVal)`, leaving `xBlock`/`yBlock`/`positionBlock`/`openingSizeBlocks`/target fields/fade fields untouched for all four directions — left/up never had the bug since they don't touch a coordinate on width change. Runtime rendering (`drawTunnelDarkness` in `gameRoomHelpers.ts`) and geometry helpers (`transitionGeometry.ts`) already read `gradientWidthBlocks` independently of `xBlock`/`yBlock` and needed no changes; serialization/hydration were not touched since the field semantics didn't change, only where it was allowed to mutate other fields. Added `src/tests/transitionGradientWidthPosition.test.ts` (20 tests) covering all four directions × widen/narrow/clamp-to-minimum/legacy-omitted-value, multi-selection edits, undo/redo, no-op resubmission, and copy/paste — asserting `xBlock`, `yBlock`, `positionBlock`, `openingSizeBlocks`, `targetRoomId`, `targetSpawnBlock`, `fadeColor`, `gradientOpacity`, `isSecretDoor`, and `longTransition` are all byte-for-byte unchanged after width edits. Validated via `npm run build` (clean) and the full `npm test` suite — all tests pass except one pre-existing failure in `roomRenderChunkWarmScheduler.test.ts` caused by an unrelated uncommitted in-progress change to `addZoneEntryViewportTasks`'s signature (not touched by this task; see `nextSteps.md`). `npm run lint` has one pre-existing unrelated error in `roomLoadingIntegration.test.ts` (`no-explicit-any`), also not touched by this task. Not manually smoke-tested in a live editor session — no browser/DOM harness available in this environment; recommend manually widening/narrowing a right- and down-facing transition in the editor and confirming the door/crossing line doesn't visibly shift before considering this fully battle-tested.)
- [ ] Canonicalize paint-authored grid layers into compact, property-aware rectangles without changing cell-level editing semantics. Confirmed problem: clear/dark ambient-light blockers are stored and serialized as one object per cell; water, lava, and Time Stop fields support rectangle records but brush painting commonly leaves many adjacent 1x1 records; compatible background blocks may remain fragmented; falling blocks are authored per cell even though runtime later groups them. Introduce a shared deterministic occupancy-to-rectangle normalizer/greedy mesher for appropriate static layers, merging only identical behavior keys: ambient blockers by `isDarkFlag`, background blocks by `blockTheme + isLightBlockingFlag`, liquids by liquid kind, Time Stop by field kind, and falling blocks by `variant + blockTheme` only if authoring/runtime identity remains correct. Erasing any cell inside a stored rectangle must remove only that cell, split or re-mesh the remainder into a stable small set of non-overlapping rectangles, and never delete the whole aggregate; edge/corner/center erasure, repeated paint/erase, undo/redo, selection, copy/paste, resize, save/load, compact-schema hydration, and deterministic serialization must preserve exact occupied cells and properties. Normalize only the affected connected component or dirty bounds during editing where practical, plus a final canonical pass at save/export; preserve runtime geometry and viewport culling, and audit upgrading the dark-blocker renderer's cached horizontal spans to full 2D greedy rectangles. Explicitly do not coalesce local/scene lights, sunbeams, challenge/gate regions, or live powder/falling-sand particles; powder may use optional save-only compression only if it expands to byte-equivalent authored particle seeds. Likely files: `src/editor/editorElementTypes.ts`, `editorPlaceTool.ts`, `editorDeleteTool.ts`, `roomJson.ts`, `roomJsonSerializer.ts`, compact schema/hydrator code, `src/levels/roomAmbientBlockers.ts`, `src/render/darkBlockerOverlay.ts`, liquid/Time Stop caches, and focused editor/schema/render tests. Add deterministic tests for coalescing, property boundaries, center/edge/corner subtraction, no overlaps or missing cells, canonical round trips, stable UID/selection behavior, runtime parity, and substantial object-count reduction for large filled regions; document detailed audit decisions and any deliberately excluded dynamic systems in `nextSteps.md`, then run targeted tests, `npm run build`, `npm run lint`, and `npm test`; increment `BUILD_NUMBER` exactly once for code changes.
- [x] Protect map sketch rendering from room-edge artifact regressions. Add narrow tests or visual debug checks if feasible. (Completed in BUILD 528: documented outside room-edge suppression in `src/ui/mapSketchRenderer.ts` and added targeted automated regression tests in `src/tests/mapSketchRenderer.test.ts` for solid rooms, cavity perimeter suppression, and open-air transition doorways.)
- [x] Complete Editor Performance Phase 4 by optimizing the remaining overlay-rendering hot paths before attempting chunked incremental editor rendering. Confirmed current costs: `src/editor/editorRenderer.ts::renderEditorOverlays` passes no shared viewport bounds to most drawers; `src/editor/editorOverlayDrawers.ts::drawEditorWalls` allocates and rebuilds an `occupied` Set plus `cellOwner` Map from every solid wall cell every frame, then loops every wall; `src/editor/editorRendererHelpers.ts::drawWallTileGrid` scans every owner cell, reparses string keys, and performs neighbor string lookups without culling; custom blocks and most functions in `editorOverlayDrawers.ts` / `editorZoneDrawers.ts` traverse full collections even when offscreen. First extend `src/editor/editorPerfCounters.ts` with counters for wall-topology rebuilds/cells scanned, overlay elements visited versus drawn, Surface Rim layout rebuilds, and **all** editor-side `editorRoomDataToRoomDef` calls so measurements include hidden paths rather than only `liveEditorRoomDef`. Compute visible block bounds once per frame (with a small margin for outlines, handles, markers, and ranged visuals) and pass a shared viewport object into overlay drawers. Cache wall occupancy/ownership topology, but do **not** key it only on `roomContentRevision`: continuous paint intentionally defers that revision until release, while `editorContentRevision.ts` increments `mutationSerial` on every mutation; use `mutationSerial` as the safe initial key or preferably introduce a dedicated `wallGeometryRevision` changed only by wall geometry/room-dimension mutations. Cull whole wall rectangles before drawing and make seam/grid rendering visit only visible cells; then cull the highest-volume collections first (custom/background/pixel-material/crumble/falling/phantasmal blocks, zones, decorations) before low-volume markers. Include `src/editor/editorWallSurfaceRimPreview.ts`: it currently computes an O(walls) signature each draw, runs `editorRoomDataToRoomDef(room)` plus `buildRoomWallTemplate` when walls change, and renders the full room; build the required snapshot directly from `EditorRoomData` or a shared wall-derived cache and pass visible row/column bounds into `renderSurfaceEdgeOverlayPass`. Separately change-gate idle hover resolution in `editorTools.ts` / `editorController.ts` by cursor block plus relevant content/layer/selectability revisions before considering a spatial index. Acceptance criteria: live paint/erase, drag, resize, selection, hover, transition linking, undo/redo, and Surface Rim previews remain immediately correct with no stale adjacency or one-frame disappearance; no room schema/save-format changes; static frames rebuild zero wall topology/layout; offscreen collections are skipped according to counters; capture before/after large-room counter and frame-time measurements; defer a chunk system unless measurements after this pass still justify it. Add focused cache-invalidation/culling/counter/hover tests, then run targeted editor tests, `npm run build`, `npm run lint`, and `npm test`; increment `BUILD_NUMBER` exactly once for the coherent code change. (Completed in BUILD 531: introduced dedicated `wallGeometryRevision` tracking in `editorContentRevision.ts` to isolate wall topology and Surface Rim layout caching from non-wall brushwork, verified comprehensive overlay viewport culling, and expanded test coverage in `src/tests/editorPerfPhase4.test.ts` and `src/tests/editorContentRevision.test.ts`.)
- [x] Decouple editor block identities from compact room-schema grouping so every hydrated logical block can be selected, moved, copied, edited, and deleted independently instead of inheriting the identity of a dehydrated aggregate. (Completed in BUILD 545: added an editor-load normalization boundary rather than touching the shared hydrator itself. `src/levels/roomSchemaHydrator.ts` now factors `hydrateByThemeBulkLayer`/`hydrateV1ByThemeLayer` internal helpers; the existing `hydrateSolidsByTheme` (runtime fast path) is unchanged in behavior/perf. Added a new `hydrateSolidsByThemeForEditor(solids)` that additionally splits every `solids.v1ByTheme` run into independent 1x1 `RoomJsonWall` entries (one per occupied cell) while leaving `byTheme` bulk rects/runs/points untouched. `hydrateV2Room(saved, opts?)` gained an opt-in `{ forEditor: true }` flag (default off, zero cost/behavior change for all existing runtime callers) that routes through the new function; `src/editor/campaignStore.ts::getRoom` — the sole production call site that feeds `jsonToEditorRoomData` (confirmed via full-codebase call-site audit) — now passes `{ forEditor: true }` so every occupied 1x1-grain wall cell gets its own `EditorWall` UID on editor load, while `exactWalls`/`specialWalls` (true 2x2 sprites, platforms, ramps, stairs, half-width pillars, Surface Rim overrides) and bulk `byTheme` walls keep their existing multi-cell semantics untouched — they never carried 1x1-grain provenance. Audited background blocks (`dehydrateBgLayers`/`saved.bgLayers`): unlike walls, background blocks have no v1-grain/bulk provenance split at all in the schema (every authored footprint, 1x1 or larger, funnels through the same lossy rect/run/point compressor), so splitting them safely is not yet possible without a schema change; left unchanged and documented as follow-up in `nextSteps.md` and a new Todo item below. Also fixed a pre-existing, unrelated build break in `src/editor/editorUI.ts` (missing `sidebarsSwapped?: boolean` on `EditorSessionUIState`, blocking `npm run build`) discovered while validating this change. Added `src/tests/editorBlockIdentityDecoupling.test.ts` (9 tests: runtime hydration unaffected, editor split of a v1ByTheme run, mixed bulk/v1/point layers, full adjacent-1x1 round-trip with independent UIDs and per-block delete, atomic 2x2/platform/ramp/stairs/half-pillar/Surface-Rim preservation, bulk `byTheme` preservation, deterministic re-save producing identical compact output, background-block behavior documented, and theme/geometry-preserving round trip). Updated `src/tests/connectedRoomPersistence.test.ts` expectations: a fresh (uncached) editor session now correctly restores a saved wBlock=2/hBlock=1 v1-compressed wall as 2 independent EditorWalls instead of 1. Validated: 2518/2518 tests pass, `npm run build` clean, `npm run lint` clean.)
- [x] Remove the duplicate legacy player-mote presentation and fix the canonical mote/HP HUD sprite aspect ratio. Repro: the intended warm-gold motes derived from player hit points remain gold, but an additional set of tiny 1-native-pixel motes follows/orbits the player and changes color when the selected mote type changes; the HUD also shows an obsolete lower row of square queue indicators, while the intended Dust Container life-slot sprites are visibly stretched instead of retaining their square pixel-art proportions. The duplicate world motes are strongly accounted for by current source: full room-load Phase B in `src/screens/gameLoadRoomPhases.ts` still calls `spawnWeaveLoadoutParticles(...)` or `spawnClusterParticles(...)` for the player and then `initMoteQueueFromParticles(...)`; `src/sim/clusters/binding.ts` binds ordinary mode-0 player-owned particles to the player; and `src/render/particles/pixelLockedDustRenderer.ts` draws those ordinary particles as 1×1 motes using the selected dust kind's palette. Resident hot swaps can also preserve and recreate them because `src/screens/playerTransfer.ts` captures player-owned non-transient particles and restores them with `behaviorMode = 0`. The intended canonical system is `src/sim/stormweave/lifeMotes.ts` plus `src/render/stormweaveLifeMoteRenderer.ts`, whose count is derived from `healthPoints` and whose head is the warm-gold 2×2 sprite/glint. Required behavior: only the canonical health-derived Stormweave life motes may idle/follow the player; changing selected/bound dust type must not spawn, recolor, transfer, respawn, or render a second player-following cloud. Remove ordinary legacy player orbit-particle spawning from both full-load and resident-transfer paths, and remove the obsolete ordered Mote Queue row from `src/screens/gameHudRenderer.ts`; do not leave invisible simulation particles or queue state running solely to support removed visuals. This is the immediate visual/player-spawn slice of the broader deferred ordered-queue/legacy-weave migration: audit every remaining consumer of `src/sim/motes/orderedMoteQueue.ts` and migrate any still-supported bow, sword, shield, dust-switch, or grapple behavior to its current authoritative system rather than silently breaking an active ability. Preserve legitimate non-orbit particles such as grapple-chain particles, launched attacks/projectiles, dust-switch travel effects, enemy-owned particles, environmental/background particles, and temporary VFX; filter by ownership/behavior/purpose rather than deleting all player-owned particles indiscriminately. For the HUD distortion, `src/render/hud/moteLifeSlots.ts` currently defines `MOTE_LIFE_SLOT_WIDTH_PX = 10` and `MOTE_LIFE_SLOT_HEIGHT_PX = 11`, and `gameHudRenderer.ts` passes those unequal dimensions to `drawAnimatedDustContainer(...)`; replace this with one square destination size and verify `dustContainerAnimation.ts`, the virtual-canvas-to-device upscale, CSS sizing, challenge-shield placement, and subsequent HUD rows all preserve a uniform nearest-neighbor square aspect at every window size/DPI. Acceptance criteria: current and maximum mote counts remain authoritative and synchronized across player health, canonical world motes, Shield/Bow/Sword consumers, and HUD slots; no load, respawn, room transition, hot swap, dust-type change, or depletion/regeneration path brings back 1×1 orbiters or the legacy queue HUD; canonical motes remain warm gold; Dust Container life slots render square and pixel-crisp with no horizontal or vertical stretching; and no save schema/progression math changes occur. Add focused tests for zero ordinary orbit-mode player particles after full load and resident transfer, preservation of allowed special-purpose player particles, dust-type changes not affecting canonical life-mote color/count, no legacy queue HUD draw, square `drawImage` destination dimensions/layout, transition/respawn lifecycle, and active weave regressions. Manually verify the reported screenshot scenario at multiple zooms, device pixel ratios, graphics qualities, and with several selected mote types, then run targeted mote/render/transfer/weave tests, `npm run build`, `npm run lint`, and `npm test`; increment `BUILD_NUMBER` exactly once for the coherent code change. (Completed in BUILD 534: removed ordinary legacy mode-0 player orbit-particle spawning and resident-transfer persistence, fixed mote life slot sprite aspect ratio to square 10x10px in HUD, updated orderedMoteQueue to derive authoritative count cleanly from canonical player health without physical orbit particles, and added comprehensive test coverage in `src/tests/canonicalMotePresentation.test.ts`.)
- [x] Recharge the player's grapple at the water surface, but never fully underwater. Acceptance criteria: when the player overlaps a non-frozen water zone and **any portion of the player hitbox is above that zone's top surface** (`player.positionYWorld - player.halfHeightWorld < world.playerBuoyancySurfaceYWorld`), restore `world.hasGrappleChargeFlag` exactly as ground contact does, including the existing golden recharge-ring effect only on the depleted→charged transition. Do not recharge when the entire hitbox is at or below the water surface, even if a shallow zone produces `PLAYER_WATER_STATE_SURFACE`; do not recharge outside water; do not repeatedly restart the ring every surface tick; and do not change underwater grapple consumption, water movement, skipping, or frozen-water behavior. Use the current tick's post-movement hitbox/water contact so crossing into the surface condition can recharge before the grapple constraint phase. Likely files: ground recharge currently lives in `src/sim/clusters/movement.ts`; authoritative AABB water detection and `playerBuoyancySurfaceYWorld` are in `src/sim/hazards.ts`; tick ordering is in `src/sim/tick.ts`; water-state constants/helpers are in `src/sim/clusters/playerWaterPhysics.ts`. Prefer a shared grapple-charge recharge helper rather than duplicating the ground transition/VFX logic. Add deterministic tests (likely extending `src/tests/playerWaterPhysics.test.ts` or a focused grapple-recharge test) for partial surface overlap, fully submerged, outside water, shallow-zone false positives, same-tick surface entry, and one-shot recharge VFX. Run targeted tests, then `npm run build`, `npm run lint`, and `npm test`. (Completed in BUILD 535: added rechargeGrappleCharge shared helper in grappleShared.ts and invoked it in hazards.ts when player hitbox extends above a non-frozen water surface after movement; extended playerWaterPhysics.test.ts with full deterministic test suite.)
- [x] Make the player's movement-bubble trail persist naturally after exiting water and constrain every bubble to the actual animated water volume/surface. Confirmed current root causes: `src/render/liquidRenderer.ts::renderWaterZones` continues ticking `tickPlayerWaterBubbles(...)` while the room contains water but calls `drawPlayerWaterBubbles(...)` only when `world.isPlayerInWaterFlag === 1`, so the entire live pool disappears visually the instant the player exits; `src/render/playerWaterBubbles.ts` also spawns around the player's center and rises without water-body or top-edge ownership, allowing a partially submerged player swimming along the surface to emit bubbles into air and allowing existing bubbles to rise above the water. Required behavior: leaving water stops **new emission only**; already spawned bubbles remain visible and continue aging/fading through their normal lifetime. Associate each spawned bubble with the containing water `LiquidBody` and exposed `topEdgeRun` (or an equivalent authoritative water-volume query), reject or clamp candidate spawn points that are outside water, and never render bubbles over air, gaps, lava, or an unrelated/disconnected water body. As bubbles rise, stop/settle them at their corresponding exposed surface rather than letting them cross into air; while resting there, preserve them for the rest of their lifetime and update their Y position to ride both the base procedural surface wave and active ripple disturbance (`getDisturbanceOffsetAt`). If drift reaches the end of a valid surface run, clamp it, transfer it only across genuinely contiguous water, or expire it—never bridge an air gap. Add explicit room/effect lifecycle reset handling so this module-level cosmetic pool cannot leak bubbles across room changes or into rooms with no water. Keep the system render-only with no gameplay, save-schema, or deterministic-simulation changes. Likely files: `src/render/playerWaterBubbles.ts`, `src/render/liquidRenderer.ts`, `src/render/liquidBodyCache.ts`, and `src/render/waterSplashSystem.ts`; preferably extract a shared wave-height sampler from the currently private `drawWaveLine` / `drawWavePath` formula so surface rendering and attached bubbles cannot diverge. Make the bubble system instanceable or expose narrow reset/test snapshots, then add focused tests for exit persistence, no out-of-water emission, partially submerged surface-spawn clipping, rise-to-surface settling, base-wave/ripple following, disconnected runs/gaps, room reset, lifetime expiry, and pool bounds. Run targeted render/water tests, then `npm run build`, `npm run lint`, and `npm test`; increment `BUILD_NUMBER` exactly once for the coherent code change. (Completed in BUILD 538: `liquidRenderer.ts` now draws the player bubble pool unconditionally each frame — leaving water only gates `tickPlayerWaterBubbles`'s new-emission branch, not the persisting pool's draw/tick path. `liquidBodyCache.ts` gained an exported `sampleLiquidSurfaceOffsetWorld` — the single shared wave+taper+disturbance formula now used by both the rendered surface and bubble settling. `playerWaterBubbles.ts` binds each bubble to its owning `LiquidBody` index and `TopEdgeRun` at spawn (via a new `findWaterRunAt` ownership query built on `LiquidBody.tileSet`/`encodeKey`), rejects/clamps spawn candidates to real water, clamps horizontal drift to the run's bounds, and settles rising bubbles onto the sampled animated surface instead of crossing into air. Added `resetPlayerWaterBubbles()`, wired into `gameRoomHazards.ts` alongside `markLiquidBodiesDirty()` on every room (re)load. Added `src/tests/playerWaterBubbles.test.ts` covering speed-gated spawn, exit persistence with continued aging/draining, out-of-water rejection, rise-to-surface settling, and room-reset clearing.)
- [x] Replace the player's ordinary-air terminal fall-speed cap with a two-stage, unbounded natural fall curve. Current behavior is implemented in `src/sim/clusters/playerVerticalMovement.ts`: normal gravity (`900 px/s²`) is applied and then downward velocity is clamped to `NORMAL_MAX_FALL_WORLD_PER_SEC` (`160.5 px/s`, effectively the requested 160); committed fast-fall instead clamps to `FAST_MAX_FALL_WORLD_PER_SEC` (`240 px/s`). Required behavior with no other forces or intentional movement mechanics acting: accelerate quickly under the existing normal gravity until reaching approximately `160 px/s` downward, then continue accelerating downward forever at exactly `20 px/s²`—about `180 px/s` after one additional second, `200 px/s` after two, etc.—with no terminal cap. Make the threshold transition frame-rate-independent using `dtSec`, and handle the crossing tick without overshoot-dependent behavior. Never reduce/clamp an already higher downward velocity caused by a grapple, zip, spring, knockback, room transfer, or another mechanic; while ordinary freefall resumes, preserve that velocity rather than snapping it back to 160. Water physics, grounded state, wall slides, upward/rising motion, active grapple constraints, and collision responses must remain authoritative and must not receive the new post-threshold acceleration when their existing logic says otherwise. Deliberately reconcile the existing down-input fast-fall and jump-held upward-brake behavior: intentional fast-fall may still accelerate the player downward faster and intentional braking may still reduce speed, but neither should leave a passive hard terminal cap that prevents subsequent natural long-fall acceleration. Likely files: `src/sim/clusters/movementConstants.ts` (current gravity/caps/debug overrides and comments), `src/sim/clusters/playerVerticalMovement.ts` (authoritative gravity/cap/brake logic), and any debug-panel bindings or tests referencing `normalFallCapWorld` / `fastFallCapWorld`; preserve backward compatibility for debug controls or rename them coherently to threshold/tuning controls. Add deterministic tests for: rapid approach to the ~160 threshold; +20 px/s after one second and +40 after two; very long unbounded falls; equivalent results across timestep subdivision; exact threshold-crossing behavior; pre-existing velocity above 160 not being reduced; landing/jumping reset behavior; water, wall-slide, active-grapple, fast-fall, and upward-brake interactions. Run targeted movement tests, then `npm run build`, `npm run lint`, and `npm test`. (Completed in BUILD 543: introduced `LONG_FALL_ACCEL_WORLD_PER_SEC2 = 20.0` in `movementConstants.ts` and `longFallAccelWorld` debug override key; replaced the hard-clamp logic in `playerVerticalMovement.ts` with a two-stage branch — stage 1 uses normal gravity up to the threshold, stage 2 cancels the gravity delta and applies only `longFallAccel` so velocity grows unboundedly at 20 px/s²; fast-fall retains its hard ceiling at `FAST_MAX_FALL_WORLD_PER_SEC`; grapple and water paths are unaffected; pre-existing high velocities are never reduced. Added 16 deterministic tests in `src/tests/playerLongFallCurve.test.ts` covering all acceptance criteria. Validated: 2509/2509 tests pass, `npm run lint` clean, `tsc --noEmit` clean, `npm run build` clean.)
- [x] Make Room Dimensions edge-resizing truly side-anchored and coordinate-complete. (Completed in BUILD 546: rewrote `src/editor/editorRoomResize.ts::applyEdgeResize` to shift the map origin inversely on top/left resizes (`mapX -= clampedDelta` / `mapY -= clampedDelta`), translate every surviving room-local spatial coordinate by the same delta, and never touch the map origin or content on bottom/right resizes. Added generic `shiftAndKeepPoint`/`shiftAndClipRect` helpers applied across every spatial collection in `editorHistory.ts::COLLECTIONS` plus nested/non-block-unit geometry: point collections (enemies, tombs, totems, dust/container entities, jars, springboards, breakable blocks, dust piles, decorations, ambient light blockers, light sources, falling blocks, phantasmal tiles, grapple-carry blocks), footprint-aware entries (spikes via size, custom block placements via tileWidth/tileHeight, pixel materials in native-pixel units scaled by `BLOCK_SIZE_SMALL`), rect collections (interior walls, challenge fields/gates, gates, water/lava zones, time-stop fields, crumble blocks, bounce pads, kinetic/zip-move blocks, grasshopper/firefly areas, background blocks, dialogue triggers), scene lights (world-unit coordinates scaled by `BLOCK_SIZE_MEDIUM`), sunbeams, ropes (both anchors, removed if either leaves bounds), guide-dust paths (all points, whole path removed if any point leaves bounds), and campaign spawn (passed in explicitly from `editorController.ts` since it's owned by the campaign session, shifted/clamped like player spawn only when its `roomId` matches the resized room). Geometry intersecting a shaved-off strip is now clipped (rects) or removed (points/lines/splines) instead of being clamped/piled onto the new edge; growing an edge only shifts, never clips. Transition opening/gradient-depth fields keep their existing correct per-axis/per-direction shift semantics (content-relative axis shifts, boundary-pinned axis doesn't) with re-clamping of opening size/position to the new bounds. Fixed a real history bug in the process: the undo/redo snapshot is now committed only after all mutation (shift/clip/map-origin) completes, so one undo/redo step restores dimensions, mapX/mapY, campaign spawn, and every shifted/clipped element atomically — previously the snapshot committed right after the width/height change, before the shift logic ran. `applyRoomDimensionChange` (used by the separate direct width/height property-edit flow, which has no "which edge" concept) is unchanged. Added `src/tests/editorRoomResizeCoordinateComplete.test.ts` (20 tests) covering opposite-edge mapX/mapY stability on grow and shrink, bottom/right non-shift, ambient-light-blocker shift and strip-removal, scene-light world-unit scaling, pixel-material pixel-unit scaling, rope dual-anchor shift/removal, guide-dust-path point shift/removal, interior-wall rect clipping vs full removal, transition axis-specific shift correctness (including the boundary-pinned non-shift case), campaign-spawn shift when owned by the resized room vs. left alone for a different room, and one-step atomic undo/redo across dimensions + mapX + shifted elements. Validated via `npm run build`, `npm run lint`, and `npm test` — 2538/2538 tests pass (2518 pre-existing + 20 new). Not manually verified in a live browser editor session (no DOM/jsdom harness in this environment) — validated at the data-transformation layer only; a manual smoke test (resize each edge on a room with transitions, ambient darkness, ropes, and a campaign spawn, then check Zone Map/Itemized Map agreement and undo/redo) is recommended before considering this fully battle-tested in a live session.)
- [x] Add a player-death disintegration effect that turns the player into roughly 80 warm-gold dust motes and blows them organically to the left. (Completed in BUILD 547: added `src/render/playerDeathDust.ts` — a render-only, bounded typed-array pool (`PlayerDeathDustEffect`, `MAX_PLAYER_DEATH_DUST_MOTES = 96`) with a deterministic LCG PRNG, following the pattern in `src/render/skidDebrisRenderer.ts`/`src/render/dustContainerPickupEffect.ts`. On the alive→dead transition, `triggerPlayerDeathDustFromSprite` in `src/screens/gameScreen.ts` resolves the player's final displayed sprite via the existing `getCharacterSprites`/`getPlayerSprite` selection, rasterizes it to an offscreen canvas, samples its full opaque-pixel silhouette (`findOpaqueOffsets`), and picks ~80 pixel offsets (`sampleOffsets`) converted to world-space via `spriteOffsetToWorld` (mirrors `renderClusters`' exact draw transform, including facing-left mirroring around `PLAYER_SPRITE_PIVOT_X_WORLD`). Each mote gets an independently randomized leftward-cone velocity, then drag/turbulence/lifetime/fade during `update(dtMs)`. Rendered in `src/screens/gameRender.ts` immediately after sunrays and before `renderWalls`, so solid foreground tiles occlude it and the dead player sprite is never drawn (already skipped by `renderClusters`'s `isAliveFlag === 0` guard). To keep the burst animating while gameplay is frozen behind the death overlay (previously the dead-frame branch returned before ever calling `renderFrame` again, so the burst rendered exactly once and then went fully static), `gameScreen.ts` now caches the exact args object passed to the last live `renderFrame(...)` call (`lastRenderFrameArgs`) and, on every subsequent dead frame, calls `playerDeathDust.update(elapsedMs)` (real frame delta, not `world.tick`) followed by `renderFrame(lastRenderFrameArgs)` — redrawing the identical frozen scene while only the dust pool's own state advances; `world.tick`, physics, input, enemies, and timers remain untouched. Reset via the existing `loadRoom()` reset block (covers new game, save-slot load, Return to Last Save respawn, and room transitions — mirrors `dustContainerPickupEffect.reset()`). Removed the prior simplified sim-particle-based approach (`src/sim/clusters/playerDeathDisintegration.ts` and its test), which used real `WorldState` Golden particles instead of a render-only effect and — because gameplay ticking stops immediately once dead — never actually animated past the single spawn frame. Added `src/tests/playerDeathDust.test.ts` (18 tests) covering opaque-pixel scanning, deterministic sampling/cycling with fewer-than-80 source pixels, the exact sprite-to-world coordinate transform including facing-left mirroring, exact mote count and bounded-pool ceiling, timestep-independent full-lifetime expiry (100×20ms vs one 2000ms step), one-shot/reset/re-trigger-without-leak semantics, and render draw-call counts. Validated: 2550/2550 tests pass, `tsc --noEmit` clean, `npm run build` clean, `npm run lint` clean. Not manually verified in a live browser — no DOM/canvas harness in this environment; the redraw-on-every-dead-frame approach is code-reviewed against the existing render pipeline but the visual "cloud blows away naturally" feel is unconfirmed. A manual smoke test (die at multiple zooms/graphics qualities, confirm the dust drifts left and fades behind walls while the death overlay fades in, then respawn/room-transition and die again to confirm no leaked motes) is recommended before considering this fully battle-tested.)
- [x] Add a deterministic post-load fade sequence for every **full campaign gameplay entry or re-entry**—starting a new campaign, loading a save slot, Return to Last Save after death, campaign restart, and any equivalent path that rebuilds/reloads the player—while leaving ordinary same-session room-to-room transitions on their existing fast path. (Completed in BUILD 536: new `src/screens/gameEntryFadeController.ts` state machine — `idle`/`pending`/`fading-to-black`/`black-hold`/`fading-to-light` — driven by elapsed real time only, with a `tickEntryFade` loop that can cross multiple phase boundaries in one large-delta call. New DOM `src/screens/gameEntryFadeOverlay.ts` (z-index above `GameLoadingOverlay`) is driven by explicit per-frame alpha, not a CSS transition. `gameScreen.ts` arms the sequence at every `startGameScreen()` call (new game / load save / restart / custom campaign all construct a fresh screen instance) except an immediate editor-playtest open, and re-arms it via the existing `onResetTransitionReveal` hook already wired to `executeGameDeathRespawn`'s Return to Last Save path. The fade is only ticked after every existing readiness gate (initial zone load, async/cross-zone room load, entry warm, sprite/background decode hold) falls through to the normal gameplay path, so ordinary room-to-room transitions — which never arm the state — are unaffected. `lastTimestampMs` is reset and buffered edge-triggered input flags are cleared (new `clearAllTriggeredInputFlags` in `src/input/handler.ts`) on the exact frame `fading-to-light` begins. Validated via 11 new deterministic tests in `src/tests/gameEntryFadeController.test.ts`, full `npm test` (2378 passing), `tsc --noEmit`, `npm run lint`, and `npm run build`.)
- [x] Add a one-shot golden-mote collection burst for Dust Containers and Dust Container Shards. Confirmed current pickup transaction is `src/screens/gamePickups.ts::processRoomPickups`: a full container permanently grants four mote-capacity slots and fills them, while a shard restores one current mote, persists one shard, and every four shards automatically forge a container. On each successful first-time pickup, trigger a **render-only** effect at the collectible's world-space center: a full Dust Container emits exactly **16** cosmetic golden motes and a Dust Container Shard emits exactly **4**. Use the existing Stormweave life motes in `src/render/stormweaveLifeMoteRenderer.ts` / `src/sim/stormweave/lifeMotes.ts` as the visual reference (warm-gold body/glint and short trail language), but make these pickup motes approximately **50% smaller**—the current life-mote head is a 2×2 native-pixel gold square with a 1×1 highlight, so target a crisp ~1×1 native-pixel head and scale any trail/glow accordingly. Each mote must launch from the container/shard with an independently randomized radial vector and a varied, decently high initial speed; use a deterministic visual PRNG and expose tuning constants rather than wall-clock randomness. For the first **1.0 seconds**, let the burst travel outward with dt-based integration and modest drag/variation. At 1.0 seconds, transition every surviving mote into very strong homing toward the player's **current rendered center**, updating the target as the player moves so the motes converge rapidly rather than aiming at a stale pickup-time position. Render this effect inside the room clip immediately before `renderClusters(...)` in `src/screens/gameRender.ts` (the same behind-player ordering principle used by `renderDustSwitchTrails`) and remove each mote once it reaches an inner player/sprite absorption radius, so it visibly disappears behind the player's sprite instead of popping in front. These particles must never become real `WorldState`/combat particles and must not change health, maximum capacity, damage, collision, weave behavior, save data, persistence keys, or shard-forging math. Collecting the fourth shard should produce only that shard's 4-mote burst; do **not** add another 16-mote burst for the automatically forged container. Prefer having `processRoomPickups` return or callback a small pickup-event payload (`container`/`shard`, world X/Y) so `gameScreen.ts` can own an instance-local bounded effect renderer (for example `src/render/dustContainerPickupEffect.ts`) without coupling deterministic simulation/progression code to Canvas/DOM. Keep the pool capped and allocation-free during update/render, freeze it while gameplay is paused, and reset it on room load/activation, respawn, new campaign/run, main-menu exit, and screen teardown so particles cannot leak between rooms or sessions. Add focused tests for exact 16/4 spawn counts, one-shot triggering and no repeat after persisted collection, randomized direction/speed variation, the exact 1-second outward→homing phase transition across timestep subdivisions, tracking a moving player, absorption/removal behind the player, bounded-pool recycling, lifecycle resets, fourth-shard behavior, and unchanged pickup/progression transactions. Manually verify the half-size visual, outward burst speed, rapid return, player occlusion, and all graphics-quality modes, then run targeted pickup/effect tests, `npm run build`, `npm run lint`, and `npm test`; increment `BUILD_NUMBER` exactly once for the coherent code change. (Completed in BUILD 536: added `src/render/dustContainerPickupEffect.ts` — a bounded, allocation-free typed-array pool with a deterministic PRNG, 1.0s outward-drag phase followed by strong live-player homing and absorption-radius removal; `gamePickups.ts::processRoomPickups` now takes an optional `onPickupBurst` callback fired once per first-time container/shard pickup (not on the auto-forge), keeping sim/progression code Canvas-free; wired into `gameScreen.ts`/`gameRender.ts` with per-tick update and rendering immediately before `renderClusters` alongside `renderDustSwitchTrails`, and reset alongside the other render-local resets in `loadRoom()`. Added `src/tests/dustContainerPickupEffect.test.ts` covering exact 16/4 counts, one-shot firing with no extra forge burst, direction/speed spread, the 1.0s phase transition across timestep subdivisions, moving-target homing/absorption, bounded-pool recycling, and reset.)
- [x] Add an editor **Swap Menu Sides** control that exchanges the two existing menu groups between the physical left and right sidebars. (Completed in BUILD 540: introduced stable `leftContentGroup`/`rightContentGroup` wrapper `<div>`s in `src/editor/editorUI.ts` beneath the fixed `#editor-ui`/`#editor-ui-right` shells; all previously hardcoded `container.appendChild(...)`/`rightSidebar.appendChild(...)` calls for room/global controls, settings, layers, inspector, export (left) and tools, brush, categories, palette (right) now target these wrappers instead. Added a compact `⇄` "Swap Menus" button in both sidebar header rows (next to each hide arrow), wired to a shared `swapMenuSides()` that re-parents the two wrapper elements between `container` and `rightSidebar` via `appendChild` (no DOM rebuild, so focus/scroll/expansion/selection state survive). Physical-edge chrome (hide arrows, reveal tabs, `getSidebarVisibility()`, `editorUIHitRegions.ts`) is untouched by the swap since it stays attached directly to the fixed shells. Extended `EditorSessionUIState` with a backward-compatible `sidebarsSwapped: boolean`, threaded through `getSessionUIStateSnapshot`/`applySessionUIState` (falls back to `false`/unswapped for older snapshots), restored via the existing `editorController.ts` close/reopen snapshot flow with no other changes needed there. Updated `src/tests/editorUISessionState.test.ts` (new swap-control/state guard) and `src/tests/editorUISidebars.test.ts` (updated append-target assertions for the new wrapper indirection). Validated via `npm run build`, `npm run lint`, and `npm test` — 2396/2396 tests pass. Not independently verified in a live browser since this project has no DOM/jsdom test harness and no separate manual browser session was available for this change; a follow-up manual smoke test of clicking both swap buttons, hiding/revealing each sidebar mid-swap, and reopening the editor is recommended before considering this fully battle-tested.)
- [x] Allow each authored room transition to use an arbitrary gradient color and independently configurable opacity instead of the current preset-only, fully opaque endpoint. (Completed in BUILD 537: added `gradientOpacity` (0..1, default 1/opaque when unset) threaded through `EditorTransition`, `RoomTransitionDef`, `RoomJsonTransition`, `SavedTransition` (as `fadeOpacity`, omitted at the default), editor↔runtime conversion (`roomJson.ts`, `editorRoomBuilder.ts`, `editorRoomImporter.ts`, `editorBackdropRoom.ts`, `roomJsonToRoomDef.ts`), compact-schema dehydrate/hydrate (`roomSchemaV2.ts`/`roomSchemaHydrator.ts`), and the transition-link/copy workflow (`editorVisualMapDialogs.ts`). Added `addColorPickerField`/`addOpacityField` widgets in `editorFormWidgets.ts` and wired a native `<input type="color">` plus a 0-100% opacity slider into `editorInspector.ts` for both single- and multi-selection, keeping the existing preset dropdown as a convenience shortcut. `editorPropertyChange.ts` now validates/clamps `transition.fadeColor` (canonical `#RRGGBB`, else falls back to black) and `transition.gradientOpacity` (clamped to `0..1`). `drawTunnelDarkness` in `gameRoomHelpers.ts` now applies the normalized opacity (via new exported `clampGradientOpacity`) to the opaque gradient stop instead of hard-coding alpha `1`, for every direction. Added `src/tests/transitionGradientOpacity.test.ts` covering clamp/normalization, JSON→RoomDef flow, compact-schema round trips (including legacy-default omission), editor property-change validation, and exact canvas gradient-stop alpha values for all four transition directions. Validated via `npm run build`, `npm run lint`, and `npm test` — 2395/2395 tests pass.)

- [x] Add a customizable dockable/floating panel system for the campaign editor: reorder panels within a sidebar, move them between sidebars, tear them out into floating windows, drag floating windows back into either sidebar at a chosen position, and persist the arrangement per campaign. (Completed in BUILD 542. Architecture: four new modules — `src/editor/editorPanelRegistry.ts` (pure registry: `EditorPanelId`, title, default side/order for all 10 panels), `src/editor/editorPanelLayout.ts` (pure layout model + `normalizePanelLayout`/`dockPanel`/`floatPanel`/`moveFloatingPanel`/`bringFloatingPanelToFront`, enforcing exactly-one-location, no duplicates, unknown-id dropping, and default placement for newly registered panels), `src/editor/editorFloatingGeometry.ts` (pure viewport clamping guaranteeing the full header/drag region stays reachable, plus rect hit-testing), and `src/editor/editorPanelDocking.ts` (Pointer Events drag coordinator: grip-only drag with pointer capture, insertion placeholder, sidebar drop highlight, edge auto-scroll, float-on-canvas-drop, redock, bring-to-front, resize re-clamp, empty-sidebar "Drop panels here" hint, and Dock Left / Dock Right / Float non-drag fallbacks). Panels are adopted into a movable host and only ever re-parented, never rebuilt, so focus, scroll, palette cards, animated previews, and inspector edits survive every move. `editorUI.ts` now registers all 10 panels instead of hardcoding `appendChild` placement; fixed chrome (title, save/confirm bar, campaign export, map row, density indicator, dev checks, hide arrows, reveal tabs) deliberately stays pinned. `editorUIHitRegions.ts` now takes X and Y plus a per-frame `floatingPanelRects` snapshot so floating windows block every canvas gesture, and an active panel drag suppresses canvas gestures outright. Workspace preferences migrated to v2 (same storage key): added `panelLayout` and split `sidebarScrollTop` into `leftSidebarScrollTop`/`rightSidebarScrollTop`, with v1 records keeping every existing preference and their scroll value reinterpreted as the left sidebar's. Reset Workspace now also restores default panel sides/order, redocks all floating windows, and zeroes both scrolls. Tests: new `editorPanelLayout.test.ts` (25), `editorFloatingGeometry.test.ts` (13), `editorWorkspacePanelLayout.test.ts` (13), `editorPanelDockingWiring.test.ts` (17 source guards), plus expanded `editorUIHitRegions.test.ts` floating coverage and rewritten `editorUISidebars.test.ts` registry assertions — 162/162 editor tests pass, `npm run build` and `npm run lint` clean. NOT verified in a live browser/Electron session: this repo has no jsdom harness and the sandboxed browser pane cannot composite frames, so the manual acceptance checklist — actual drag/drop feel, floating-panel usability for Palette and Inspector, scroll behavior, restart persistence, and window-resize reachability — remains outstanding. See `nextSteps.md`.)

## Deferred / medium priority

- [x] Add first-class localization/i18n support so all player-facing game, campaign, pause/settings, HUD, dialogue, editor, accessibility, and error/loading text can be translated without editing gameplay code. Introduce a centralized typed translation-key API and locale catalogs (English as the complete source and per-key fallback), replace hard-coded UI strings with keys plus interpolation/pluralization, and add a language selector that applies immediately and persists independently of campaign/save data. Use the platform locale only as a safe first-launch default; missing/invalid locales or keys must fall back to English without displaying raw keys or crashing. Preserve deterministic simulation and save compatibility; do not translate internal IDs, asset paths, debug-only identifiers, or player-authored campaign text unless an explicit localized-content schema is added. Ensure the pixel-art UI handles longer/shorter strings, Unicode, accents, non-Latin glyphs, wrapping/truncation, and font fallback without clipping; design the API so right-to-left layout can be added later without rewriting callers, even if RTL is outside the first release. Likely subsystems: a new `src/i18n/` runtime/catalog layer, main-menu and pause/settings language controls, shared DOM/canvas text renderers, HUD/dialogue/editor strings, preference persistence, and font loading. Acceptance criteria: English and at least one non-English test locale can switch at runtime across representative DOM and canvas screens; the choice survives restart; fallback, interpolation, and pluralization are deterministic and tested; a catalog-parity/extraction test or lint guard detects missing keys and newly introduced hard-coded player-facing strings; locale alone does not alter simulation/save hashes; and targeted localization tests plus `npm run build`, `npm run lint`, and `npm test` pass. Increment `BUILD_NUMBER` exactly once for the coherent implementation. (Completed in BUILD 568: Added `src/i18n/` — typed catalogs (`catalogs/en.ts` authoritative, `catalogs/es.ts` partial with per-key English fallback; `TranslationKey` derived from the English record so no untyped keys), deterministic in-house pluralization (`plural.ts`, no Intl), `{placeholder}` interpolation, locale registry with RTL-ready direction + per-locale font-fallback stacks (`locales.ts`), separate localStorage preference `stickblade-locale` with legacy-format migration (`preference.ts`), first-launch platform detection, and a synchronous locale-change subscription (`runtime.ts`) plus DOM (`domText.ts`) and canvas (`canvasText.ts`) binding/measurement helpers. Migrated main menu, save slots + assist-mode dialog, custom-campaign selection/import/create dialogs, main-menu settings (new Language tab, `src/ui/mainMenuSettingsLanguage.ts`), pause menu, death screen, character select, weave loadout, world map, loading overlay, the canvas control-hint overlay, and the editor save-changes dialog + editor header/action bar. Tests: `src/tests/i18nRuntime.test.ts`, `i18nPreference.test.ts`, `i18nUiText.test.ts`, `i18nSimIsolation.test.ts`, `i18nHardcodedStringGuard.test.ts`. Not yet migrated: most of `editorUI.ts`/editor inspector panels, debug panels, keybindings tab, skill-tomb menus, and the dialogue overlay chrome — see `nextSteps.md`.)
- [x] Add render-only constellation links between the canonical luminous Stormweave life motes. When two motes enter a configurable proximity band, draw a thin line between their centers whose opacity rises smoothly as distance decreases (fully transparent at the outer threshold and capped at a subtle maximum near the inner threshold), so nearby motes resemble stars forming constellations. Render links behind mote heads and derive their color from the current mote palette/glow; do not alter mote movement, player health, Shield Weave geometry, collision, save data, or deterministic simulation. Avoid a dense all-to-all web at high mote counts by using a deterministic bounded-neighbor policy (for example, nearest neighbors with a per-mote connection cap), prevent duplicate pair draws, and avoid frame-to-frame popping through stable pair selection and/or brief visual smoothing or hysteresis. (Completed in BUILD 567: Added `src/render/stormweaveConstellationLinks.ts` — pure `getConstellationLinkOpacity` smoothstep helper and `selectConstellationLinkPairs`/`ConstellationLinkTracker` deterministic bounded-nearest-neighbor selection with distance-ranking hysteresis for stable pair selection across frames, keyed per `StormweaveLifeMotes` instance via a `WeakMap` in `stormweaveLifeMoteRenderer.ts` so it stays render-local. Links draw first in the pass, behind trails/glow/body/Shield Weave, using the existing per-kind palette's `trailMainHex`. Quality-gated via `CONSTELLATION_LINK_QUALITY`: high (3-neighbor cap, 4-11 world units), med (2-neighbor cap, 3-7 world units), low (disabled). Added `src/tests/stormweaveConstellationLinks.test.ts` (15 tests) covering opacity thresholds/interpolation, neighbor selection, tie-breaking, per-mote caps, duplicate prevention, mote-count growth/shrink, quality gating, Shield Weave-ring positions, hysteresis stability, and bounded (non-O(n²)) output at 48 motes. Full suite (2,798 tests), build, and lint pass. Manual in-browser visual verification across mote counts/dust types/zooms/qualities was not performed this session — see `nextSteps.md`.)
- [x] Migrate/remove the legacy ordered combat-mote queue, Storm-as-equipped-primary gating, Arrow Weave, and Sword Weave without disturbing the health-derived Stormweave/Shield Weave collection; choose replacement controls before exposing legacy secondary abilities again. (Completed in BUILD 553: Purged all legacy secondary-weave runtime systems (`bowArrow.ts`, `swordWeave.ts`, `secondaryWeaveCoordinator.ts`, legacy queue-backed `weaves/shieldWeave.ts`, and `orderedMoteQueue.ts`), decoupled player mote capacity and shield weave entirely onto the canonical health-derived Stormweave (`src/sim/stormweave/shieldWeave.ts`), and removed the obsolete `particleMoteSlotState` and secondary weave controls from input paths and rendering. Legacy save file identifiers (`shield_sword`, `arrow`, `sword`, `shield`, `storm`, `none`) remain recognized solely for data migration compatibility to unlock progression abilities without exposing obsolete combat controls. Validated via full test suite (2,496 passing tests), ESLint, and production build.)
- [x] Extend Shield Weave collision adapters for liquid surfing (lava and water). Implemented geometry-based directional contact adapters for both lava and non-frozen water zones that require the active downward crescent to touch the surface, handling tunneling via a swept fallback and suppressing repeated skips via AABB overlap latches. (Completed in BUILD 552: Added `src/sim/stormweave/shieldLiquidSurface.ts` and integrated in `src/sim/hazards.ts`. Added comprehensive test suite `src/tests/shieldLiquidSurfing.test.ts`.)
- [x] Extend Shield Weave collision adapters for laser reflection only after those systems define directional contact/reflection contracts; do not approximate them through the current ordinary-projectile blocker. (Completed in BUILD 565: Added a placeable, four-direction Laser Emitter to the editor palette/hazards layer with full persistence (JSON, compact-schema tuple, undo/redo, copy/paste, resize clipping, room-complexity accounting) alongside the existing base laser sim/runtime. Added an exact circle ray-intersection + reflection contract (`getShieldArcRayHit`/`reflectDirection` in `src/sim/stormweave/shieldWeave.ts`, `src/sim/laserTraceContract.ts`) that traces each beam against the Shield Weave's real curved arc, reflects exactly once off the true contact point/normal using the standard reflection equation, and continues the outgoing leg (small epsilon offset) to the nearest terrain hit; wired per-tick into `src/sim/hazards.ts` and rendered as incoming/contact-flash/outgoing segments in `src/render/hazards.ts` with no separate renderer-side collision geometry. Added `src/tests/laserShieldReflection.test.ts` and `src/tests/editorLaserEmitter.test.ts`.)
- [x] Implement `evictStalePrewarmedChunks` with an LRU or memory cap if warmed chunk memory growth is observed. (Audited in BUILD 560: the implementation in `src/screens/roomRenderChunkWarmScheduler.ts` already satisfies this item and was retained as-is — a radius/size-ordered memory-cap policy (per-quality-tier KB budgets: low 4096 / med 12288 / high 32768), not a timestamp-based LRU. This is deliberately correct rather than a gap: prewarm rooms have no meaningful "last accessed" time (they're never accessed by the player — only speculatively built and either adopted-once or discarded), so an LRU would rank candidates on an arbitrary signal. BFS radius to the current room is the actual predictor of future value, so eviction order is radius-descending (least likely to be entered soon first), then largest-memory-first within a radius tier — both already implemented before this task. The eviction path runs at three lifecycle points: schedule start (`scheduleChunkPrewarms`), after any idle slice that builds chunks (post-slice enforcement in `_runSlice`), and on-demand via `invalidateRoomChunkPrewarm` for editor-driven cache invalidation. Found and fixed one real defect: `invalidateRoomChunkPrewarm` unconditionally read `import.meta.env.DEV`, which throws outside a Vite-provided `import.meta.env` (e.g. any plain Node invocation) — changed to the `import.meta.env?.DEV` pattern already used elsewhere in the codebase (`src/debug/perfFreezeProfiler.ts`). No other accounting, lifecycle, or double-counting defects were found across wall/bg cache creation, adoption, invalidation, schedule restart, graphics-quality changes, or stats reporting. Added 10 new focused tests to `src/tests/roomRenderChunkWarmScheduler.test.ts` covering background-only cached rooms, combined wall+bg memory accounting without double-counting, stale eviction clearing both stores together, active-room protection even far over budget, per-quality-tier budget selection, eviction stats accuracy and stability across repeated calls (no re-eviction of already-gone rooms), post-slice budget enforcement, invalidation + re-queue behavior, and newly-completed nearby rooms surviving eviction while still in the BFS keep-set. Validated: 14/14 focused tests pass, 2675/2675 full suite passes, `npm run build` and `npm run lint` clean. No profiling data indicates prewarm memory growth is currently a real-world problem; this item was closed on implementation-completeness grounds, not a measured regression. If a future profiling session shows sustained over-budget memory in production, the fix would be tightening `PREWARM_MEMORY_BUDGET_KB` or shortening `MAX_PREWARM_RADIUS`, not switching to timestamp LRU.)
- [x] Make radius-3 render chunk warming more adaptive by incorporating frame-time checks, if needed. (Completed in BUILD 561: audited `src/screens/roomRenderChunkWarmScheduler.ts::_runSlice`; frame-time/quality gating already existed (`FRAME_TIME_PAUSE_THRESHOLD_MS`, `RADIUS3_HIGH_QUALITY_ONLY`, reduced one-chunk budget) but the gate permanently discarded (`_queue.shift()`) radius-3 tasks on a single poor frame or non-high quality, so a transient slow frame or a temporary quality dip deleted work that could only be recreated by a fresh room transition, and a medium→high quality change did not resume radius-3 warming until the next transition. Changed the gate to defer radius-3 tasks (move to the back of the queue, reusing the existing per-slice `MAX_DEFERRALS_PER_SLICE` starvation guard) instead of discarding them, so a task survives poor-frame/non-high-quality windows and is automatically re-evaluated on the very next slice once frame time/quality recover, with no new room transition required. Radius-1/2 tasks are unaffected (the gate only applies to radius >= 3) so transition-critical work is never delayed behind deferred radius-3 work. Added a `deferredRadius3` stat (reset per schedule) so the deferral is observable rather than silently indistinguishable from "never queued". Left the memory-budget eviction, one-chunk reduced-frame budget, and idle-timeout skip-on-poor-frame logic unchanged — no gap was demonstrated there. Extended `src/tests/roomRenderChunkWarmScheduler.test.ts`: rewrote the discard-oriented test to assert deferral (radius-3 stays queued, `deferredRadius3` increments) for both poor frame time and med quality; added a same-schedule recovery test (poor frame → recovers → radius-3 still present, `pausedForFrameTime` clears without a new `scheduleChunkPrewarms` call); added an oscillating good/bad frame-time test proving the task is never lost and slicing does not hang. Validated: focused suite 16/16 pass, full suite 2677/2677 pass, `npm run build` clean, `npm run lint` clean. No live browser frame-time profiling was performed (deterministic audit only, per task constraints); manual verification recommended: use `__dwBenchPingPong`/`__dwTransitionStats` to confirm the debug panel's `deferredRadius3`/`pausedForFrameTime` stats behave as expected under a real transient frame hitch.)
- [x] Expose the prewarm debug panel in pause-menu debug UI if useful. (Completed in BUILD 539: added 'Prewarm Panel (debug)' option to pause menu when Debug Mode is on in src/ui/pauseMenu.ts, backed by new setDebugPanelVisible helper in src/ui/debugPanelManager.ts; also added 'prewarm' and 'freeze' toggles to DEBUG_PANEL_DEFS in src/ui/debugPanel.ts; validated with new unit tests in src/tests/debugPanelManager.test.ts.)
- [x] Add CI smoke test for `npm ci && npm run build`. (Verified: `.github/workflows/ci.yml` already runs `npm ci`, `npm run build`, `npm run lint`, and `npm test` on every push and PR targeting `main`. No code changes needed.)
- [x] Consider `BgWallGridView` dense/sparse adapter only if large-room memory pressure becomes a real issue. (Audited, not implemented — not currently justified. `bgWallGrid` is a single flat `Uint8Array(widthBlocks * heightBlocks)` field on `WorldState`/`ResidentWorld`, reallocated only when a room's cell count changes (`src/sim/world.ts`, `src/screens/gameLoadRoomPhases.ts`, `src/screens/residentWorldBuilder.ts` ×2 for normal/gen resident-build paths); exactly one instance exists per world at a time, never cloned/serialized/duplicated. Direct hot-path consumer `src/sim/clusters/snakeAi.ts` reads `world.bgWallGrid[idx]` by raw index, so any future sparse form must keep O(1) indexed access, not a string-keyed map. Measured all 33 current official campaign rooms (`ASSETS/CAMPAIGNS/STICKBLADE_CAMPAIGN/ROOMS/*_room.json`, `size` field): largest is `chasm` at 200×300 = 60,000 cells (58.6 KB dense, since `Uint8Array` is 1 byte/cell with no per-element overhead); second is `underwater_lake` at 300×161 = 48,300 cells (47.2 KB). Both are below the existing DEV-only `65536`-cell large-room log threshold in `residentWorldBuilder.ts`/`gameLoadRoomPhases.ts` — no official room has ever crossed it. Worst-case aggregate across `MAX_RESIDENTS_BASELINE = 16` simultaneously resident rooms (`residentRoomManager.ts`), using the 16 largest official rooms as an upper bound (real BFS adjacency won't cluster them this way), is ≈262,924 bytes ≈ 256.8 KB total — trivial next to per-room sprite/texture/wall-template memory. Decision: current dense representation is not a meaningful memory-pressure source; a dense/sparse adapter would add indexing complexity and a second representation to maintain for no measured benefit. Reopen if either: (a) an official room's `widthBlocks * heightBlocks` exceeds ~65,536 cells (64 KB dense), or (b) profiling shows aggregate resident-set `bgWallGrid` memory reaching low single-digit MB in practice. No source changes made; see `nextSteps.md` for the full room-by-room table.)
- [x] Add sprite assets and registrations for enemy palette previews that currently use procedural placeholders. (Completed in BUILD 544: registered `enemy_crimson_wizard` in `ITEM_SPRITE_URL` using the existing `CrimsonWizard_Idle.png` sprite asset, upgrading it from the `?` fallback to a real sprite preview. Added procedural `ITEM_VISUAL` entries for all 9 enemies that previously showed the `?` fallback: the 6 `enemy_grid_block_*` speed variants (colour-coded teal/amber/red with '1'/'2' size glyphs), `enemy_herald` (dark void orb with purple border), and `enemy_ice_wizard` (ice-blue diamond). All changes are in `src/editor/editorPalettePreview.ts`. Validated: 2509/2509 tests pass, `npm run lint` clean, `npm run build` clean.)
- [x] Add crumble/falling block palette entries if those block types become active editor items. (Investigated in BUILD 559: superseded by the existing Block Modifier system — standalone crumble/falling palette cards were NOT added, since `editorUI.ts`'s "Block Modifier" panel (Cracked / Falling: Tough / Sensitive / Crumbling) already lets any eligible `blocks`-category palette item place as crumble or falling. Audit found and fixed two real UI/placement mismatches in `editorPlaceTool.ts`: (1) `isSpikeItem` items short-circuited before the crumble-modifier check, so selecting a spike with Cracked active silently placed an ordinary, unbreakable spike instead of a crumble spike — despite crumble spikes being fully supported at the data layer (`EditorCrumbleBlock.spikeDirection`/`spikeSize`, `handleCrumbleModifierToggle`, `src/tests/crumbleSpikes.test.ts`) and the placement preview already drawing the crack overlay for this case; fixed by reordering the crumble check first and extending it to capture spike direction/size. (2) The Falling modifier's implicit-trigger condition and the Cracked modifier's implicit-trigger condition did not exclude `isPlatformItem`, so a stale pending modifier carried over from a previous palette selection could silently convert a placed platform into a crumble/falling block; added `isPlatformItem !== 1` guards to both. Also restricted the Falling modifier (architecturally unable to represent ramp/stairs/pillar/spike shapes — `EditorFallingBlock` has no orientation/spike fields, unlike `EditorCrumbleBlock`) to plain `block_1x1`/`block_2x2` items only, hiding its three checkboxes in `editorUI.ts` for shaped items and resetting a stale falling selection to `'none'` when switching to one — Cracked remains available for all crumble-eligible shapes. Platforms remain fully ineligible for both modifiers. Added `src/tests/editorCrumbleFallingModifierRouting.test.ts` (28 tests) covering crumble-spike routing, falling-modifier shape restriction, and platform ineligibility. Validated: 2665/2665 tests pass, `npm run build` clean, `npm run lint` clean. Not manually verified in a live editor session — no DOM/jsdom harness in this environment; validated at the data-transformation layer only.)
- [x] Extend the compact background-block schema (`src/levels/roomSchemaV2.ts::dehydrateBgLayers`, `saved.bgLayers`, `src/levels/roomSchemaHydrator.ts`) with a 1x1-grain/bulk provenance split analogous to walls' `solids.byTheme` vs `solids.v1ByTheme`, then apply the same editor-load per-cell splitting used for walls to background blocks. (Completed in BUILD 548: `SavedBgLayer` gained an optional `v1?: Saved1x1Layer` field alongside the now-optional `layer?: SavedSolidLayer`. `dehydrateBgLayers` partitions each (themeKey, lb) group's members by `wBlock === 1 && hBlock === 1` — 1x1-authored blocks compress via `extract1x1LayerFromGrid` into `v1` (runs+points only, no rects), bulk blocks keep the existing `extractLayerFromGrid` compressor in `layer`; a group may have either field, both, or (if empty) is omitted entirely. `roomSchemaHydrator.ts` added `hydrateBgLayers` (runtime fast path — expands both `layer` and `v1`, with `v1` runs staying merged, matching `hydrateSolidsByTheme`'s wall behavior) and `hydrateBgLayersForEditor` (editor path — additionally splits every `v1` run into independent 1x1 `RoomJsonBackgroundBlock` entries, mirroring `hydrateSolidsByThemeForEditor`); both are wired into the existing `hydrateV2Room(saved, opts)` `forEditor` boundary already used by `campaignStore.ts::getRoom`, so every occupied 1x1-grain background cell now gets its own `EditorBackgroundBlock` UID on editor load while legitimately-authored bulk footprints are never split. `roomFileAudit.ts`'s bg-layer primitive counter now also counts `v1`. Updated the now-outdated assertion in `src/tests/editorBlockIdentityDecoupling.test.ts` (previously documented merged-on-reopen as expected/deferred) and added 2 new tests there for bulk-footprint preservation and theme/lb-group isolation with deterministic re-save. Validated: 2552/2552 tests pass, `tsc --noEmit` clean, `npm run build` clean, `npm run lint` clean. Not manually verified in a live editor session (no DOM/jsdom harness in this environment) — validated at the data-transformation layer only.)
- [x] Investigate menu animation lag if still reproducible with current assets and renderer path. (Verified via code audit: `src/ui/menuAnimationFrames.ts::preloadMenuAnimationFrames` fully decodes all 600 frame images (`image.decode()`) and then rasterizes every one onto a scratch canvas (`warmFrames`) *before* `src/main.ts` dismisses the loading screen, so the first real animation frame never pays first-decode/first-rasterize cost — this is the exact fix for classic "menu animation lag" (stutter on the opening loop). The steady-state per-frame cost in `src/ui/menuAnimatedBackground.ts::render` is a single cached-size check plus one `drawImage` call, with no other continuous rAF loops registered by `src/ui/mainMenu.ts`. This pipeline was already added by a dedicated prior fix (`git log`: "Fix main menu animation startup"). Could not capture live frame-timing in this sandbox's Browser pane — it reports `document.hidden === true` with no compositing, which freezes `requestAnimationFrame` entirely regardless of app behavior — so no dynamic measurement was possible; treat this as a code-level verification, not a profiled measurement. If a user still observes stutter, prior evidence should be re-collected on real hardware via `performance.now()` deltas around `requestAnimationFrame` in the browser devtools console.)
- [x] Verify and polish ultra ice behavior: wall contact stops slip, touching ultra ice resets grapple, and zero-velocity stuck cases return control. (Verified in BUILD 550 via code audit + new deterministic tests, no bug found: `src/sim/clusters/movement.ts` lines 376-416 already handle all three cases — wall contact (`isTouchingWallLeftFlag`/`RightFlag`) clears `isOnUltraIceFlag` and the shared wall-collision resolver zeroes velocity on impact regardless of surface; ground contact on an ultra-ice wall sets `isGroundedOnUltraIceFlag`, which flows through the same `rechargeGrappleCharge` call used by ordinary ground contact (no separate/divergent logic); and a locked slip velocity decaying under `ULTRA_ICE_STOPPED_SPEED_EPSILON_WORLD` (0.1) already clears `isOnUltraIceFlag` so input/control is restored. Added `src/tests/ultraIce.test.ts` (4 tests) exercising all three behaviors end-to-end through `applyClusterMovement` with a real ultra-ice-flagged wall, since no prior test covered anything beyond ultra ice's skid suppression. Validated: 2556/2556 tests pass, `npm run build` clean, `npm run lint` clean.)
- [x] Reorganize repository documentation so most root-level Markdown files move under `docs/` without breaking agent workflows or repository discoverability. (Completed in BUILD 549: moved 16 root Markdown files into `docs/decisions/` (`DECISIONS.md`, `REFACTORING_PLAN.md`, `performanceOptimizationDecisions.md`, `combatDustPolishDecisions.md`, `MajorDustUpgradePlan.md`), `docs/systems/` (`movement.md`, `CustomBlockSpriteSystem.md`, `RoomLoadingOptimizations.md`, `PERFORMANCE_DIAGNOSIS.md`, `manual_test_checklist.md`, and root `ARCHITECTURE.md` renamed to `docs/systems/render-pipeline.md` since `docs/ARCHITECTURE.md` already existed as the canonical compact AI-facing guide), and `docs/archive/` (`RefactorPlan.md`, `RoomLoadingOptimizations.local.md`, `DUST_TYPES_ARCHIVE.md`, `ENEMY_COMBAT_ARCHIVE.md`, `legacy.md` — working logs and superseded material). Removed the duplicate root `agents.md` (byte-identical to `AGENTS.md`; discovered mid-task that on this Windows checkout's case-insensitive filesystem `git rm agents.md` deleted the single underlying file shared with `AGENTS.md` on disk — recovered via `git show HEAD:AGENTS.md`, only the tracked-as-lowercase path was actually removed from the index). Deliberately kept root `README.md`, `AGENTS.md`, and `nextSteps.md` at root: `nextSteps.md` is read every task cycle per `AGENTS.md` and is cross-referenced from 7+ source files and several docs — moving it would only add churn, not reader benefit, so this is a documented deliberate exception rather than a blind move. Added `docs/README.md` as the canonical/archive index. Updated every reference to a moved path in `AGENTS.md`, `README.md`, `docs/AI_REPO_MAP.md`, `docs/CURRENT_STATUS.md`, and in source comments across `src/render/lavaSparkSystem.ts`, `liquidRenderer.ts`, `playerRocketChargeParticles.ts`, `playerWaterSkipSpray.ts`, `skidDebrisRenderer.ts`, `src/sim/pixelMaterials/pixelMaterialTypes.ts`, `pixelMaterialTick.ts`, `src/sim/physics/collision.ts`, `src/sim/motes/orderedMoteQueue.ts`, `src/sim/particles/combat.ts`, `src/sim/clusters/grappleMiss.ts`, `enemyAi.ts`, `src/levels/customBlockProperties.ts`, and `src/editor/editorRoomBuilder.ts`, plus internal cross-links inside the moved docs themselves (`docs/archive/legacy.md`, `docs/decisions/DECISIONS.md`, `docs/decisions/REFACTORING_PLAN.md`, `docs/systems/RoomLoadingOptimizations.md`, `docs/pixelMaterials.md`). Left `docs/archive/RefactorPlan.md`'s and `docs/systems/CustomBlockSpriteSystem.md`'s own historical self-references untouched (frozen changelog/working-log content, not live navigation). Also fixed a stale claim discovered in passing while auditing an adjacent deferred Todo item: `docs/CURRENT_STATUS.md` said `evictStalePrewarmedChunks` was "a no-op stub", but `src/screens/roomRenderChunkWarmScheduler.ts` already implements full radius/size-ordered, per-quality-budget eviction with existing test coverage (`src/tests/roomRenderChunkWarmScheduler.test.ts`) — corrected the doc rather than re-implementing what already exists. Since source comment paths in `.ts` files changed (not just Markdown), incremented `BUILD_NUMBER` to 549 per the `AGENTS.md` rule for any coherent codebase change, even though this Todo item's own acceptance text anticipated zero source/BUILD_NUMBER changes — the two rules conflicted once source comments needed updating, and the repo-wide `AGENTS.md` policy was treated as authoritative. Verified via repeated repo-wide `grep`/`git grep` passes after moving that no stale bare references remain outside frozen archive logs. Validated: `npm run build`, `npm run lint`, `npm test` (2552/2552) all clean — this was a path-only change with no logic touched, so no new tests were needed.)
- [x] Editor UI redesign, part 1/2: two independent 260px left/right sidebars (`#editor-ui` / `#editor-ui-right`) with the specified content order, and a reusable accessible collapsible-section component (`createCollapsibleSection` in `src/editor/editorUIHelpers.ts` — real `<button>` header + chevron + `aria-expanded`/`aria-controls`, defaults to collapsed) used by every top-level panel in both sidebars, including the layers panel (`src/editor/editorUILayersPanel.ts`, now built on the shared component). The detached top-right map bar is removed and replaced with the "Zone Map (M)" / "Itemized Map (N)" button row directly under Save and Export Campaign, wired to the existing `onOpenVisualMap` / new `onOpenWorldMap` callbacks. Validated via `npm run build`, `npm run lint`, `npm test`.
- [x] Editor UI redesign, part 2/2: in-memory, controller-owned session state (`EditorSessionUIState` in `src/editor/editorUI.ts`) snapshots every collapsible section's expanded/collapsed state (keyed via a new `key` option on `createCollapsibleSection`) plus each sidebar's visible/hidden state, captured in `closeEditor()` right before `ui.destroy()` and reapplied in `toggle()`'s isActive branch after `createEditorUI()` — survives editor close/reopen within the running session, first-ever open still defaults to all-collapsed, kept fully separate from the disk/localStorage-persisted `EditorWorkspaceUIPrefs`. Added independent hide arrows atop each sidebar plus inward-pointing edge reveal tabs (`#editor-ui` / `#editor-ui-right` operate fully independently). Replaced the hardcoded `EDITOR_PANEL_WIDTH_CSS_PX` (260) check in `editorController.ts` with shared, dynamic hit-region logic in new `src/editor/editorUIHitRegions.ts` (`isPointOverEditorUI`/`isPointOverEditorCanvas`), applied to every canvas gesture path (click, right-click delete, drag-paint, right-drag-paint, hover-scan, wheel zoom) — a hidden sidebar's old 260px region is now fully interactive again, minus only its own reveal-tab hit area. Validated via `npm run build`, `npm run lint`, `npm test`.

- [x] Fix grapple-release gold motes so they collide naturally with walls and persist across rapid re-grapples. (Completed in BUILD 557: added a dedicated 30-slot released-mote pool (`GRAPPLE_RELEASE_POOL_CAPACITY = GRAPPLE_SEGMENT_COUNT * GRAPPLE_RELEASE_POOL_GROUPS`) allocated contiguously after the active chain slots in `initGrappleChainParticles` (`src/sim/clusters/grapple.ts`), tracked via new `grappleReleaseStartIndex`/`grappleReleaseBurstCounter` fields on `GrappleWorldState` (`src/sim/worldGrappleState.ts`). `releaseGrapple` (`src/sim/clusters/grappleShared.ts`) now kills the old active chain slots and writes the release burst into a round-robin group of the dedicated pool instead of reusing the chain slots directly, so firing a new grapple only reinitializes the active chain and never erases a still-flying prior release burst; a 4th overlapping release deterministically evicts the oldest of the 3 groups. `src/sim/particles/walls.ts::applyWallForces` now also exempts unowned `ParticleKind.Gold` (alongside the existing unowned `Golden` exemption) from the soft pre-contact wall-repulsion field, so released motes travel freely until actual contact. `applyWallBounce` applies a new `GRAPPLE_RELEASE_BOUNCE_DAMPING = 0.50` specifically to unowned Gold particles on contact, leaving the generic `WALL_BOUNCE_DAMPING = 0.60` unchanged for all other particles. Released motes remain unowned (`ownerEntityId = -1`) and transient (`isTransientFlag = 1`), which was already sufficient to exclude them from binding, dust/mote counts, and resident-room transfer capture (verified by code audit, no changes needed there). Room/challenge lifecycle resets (`src/screens/gameLoadRoomPhases.ts`, `src/screens/gameRoomChallenge.ts`) now also clear the new release-pool fields alongside the existing chain reset. Added `src/tests/grappleReleaseMotes.test.ts` (7 tests) covering dedicated pool allocation, chain-slot death + release-pool spawn on release, simultaneous persistence of up to 3 overlapping release bursts, deterministic 4th-burst eviction, zero wall-repulsion force near-but-not-touching a wall, exact 50% reflected speed on contact, and unaffected 60% damping for ordinary particles. Validated: 2628/2628 tests pass (2621 pre-existing + 7 new), `npm run build` clean, `npm run lint` clean.)

- [x] Add Steam Achievements support through a narrow platform abstraction rather than coupling gameplay systems directly to Steamworks. Define stable internal achievement IDs and unlock criteria; initialize Steam only in packaged Steam builds; make unlocks idempotent, persistent, retryable when the Steam client is temporarily unavailable, and harmless in web/dev/non-Steam builds through a no-op adapter. Route progression/gameplay events through an Electron main-process IPC boundary rather than exposing Steam APIs to renderer code. Likely areas: Electron/bootstrap and IPC, packaging/build configuration, save/progression event sources, and new platform/Steam achievement modules. Add fake-adapter tests for one-shot and cumulative achievements, relaunch persistence, duplicate unlock calls, unavailable-client recovery, and non-Steam fallback; document App ID/configuration and manually verify representative unlocks in the Steam client.
  - Implemented `src/platform/` (types, achievementIds, fakeSteamAdapter, steamAdapter, ipcBridge, rendererPlatform, index), `src/progression/achievementTracker.ts`, and `electron/platformBridge.cjs` + `preload.cjs`'s `window.electronPlatform`. Save-load reconciliation in `src/progression/saveSlots.ts` (`reconcileSaveSlotAchievements`, `SaveSlotData.unlockedAchievements`). 15 fake-adapter/tracker/reconciliation tests in `src/tests/steamAchievements.test.ts`, all passing. `npm run build`/`lint`/`test` all clean (2896 tests passing). Manual Steam-client verification still required — see `docs/SteamSetup.md`.
- [x] Add Steam Workshop support for publishing, updating, discovering, subscribing to, downloading, and removing user-created StickBlade campaigns/levels. Define a versioned Workshop package and manifest format with metadata, preview image, content hashes, compatibility/migration rules, deterministic import/export, and strict path/archive/content validation. Implement Steam-only Workshop operations behind a platform adapter and Electron IPC boundary; integrate subscribed content into the existing campaign browser without overwriting local campaigns or breaking web/dev/non-Steam use. Likely areas: campaign serialization/store/browser UI, Electron main process, packaging permissions, and new platform/Steam Workshop modules. Add fake-adapter tests for publish/update, subscribe/unsubscribe, duplicate IDs, offline cached items, malformed packages, and version mismatches, plus manual Steam-client validation of the complete creator and subscriber flows.
  - Implemented `src/workshop/` (types, fakeWorkshopAdapter, steamWorkshopAdapter, rendererWorkshopAdapter, packageValidator, index), Workshop IPC channels in `src/platform/ipcBridge.ts` + `electron/platformBridge.cjs`, and `src/ui/workshopBrowser.ts` wired into `mainMenuCustomCampaigns.ts` via a "Browse Workshop" button. 25 fake-adapter + validator tests across `src/tests/workshopAdapter.test.ts` and `src/tests/workshopPackageValidator.test.ts`. Manual Steam-client verification still required.
- [x] Workshop Play flow (subscriber end-to-end): wire "Play" on an installed Workshop item into the existing `CampaignSource` loader instead of it being a no-op. Added `src/workshop/workshopCampaignLoader.ts` (`loadCampaignSourceForWorkshopItem`), which validates an installed item via the existing `packageValidator.ts` and returns a `CampaignSource` using the same `loadPackedCampaign` field packed/browser-imported campaigns already use — no parallel gameplay-loading path. Added `WorkshopAdapter.readInstalledPackage()` (implemented by `fakeWorkshopAdapter`, `rendererWorkshopAdapter` via a new `dw:workshop-read-package` IPC channel, and `steamWorkshopAdapter`/`platformBridge.cjs` reading real disk with path-traversal-safe symlink-checked directory walking) since no prior IPC surface could read a Workshop install directory's contents. Wired `onPlayItem` into `mainMenuCustomCampaigns.ts`'s `showWorkshopBrowser` call, routing a successful load through the existing `onPlayCustomCampaign` flow and showing a localized error (`workshop.playFailed`) while leaving the menu usable on failure (still-downloading, missing path, unreadable/malformed package, unsupported `formatVersion`, or removed-between-listing-and-play). See `docs/SteamSetup.md` for the remaining real-Steam-client manual verification.

- [x] Add an editor-authored **Poison Field** hazard with amorphous, faint purple-cloud visuals. Gameplay: a poison-vulnerable player takes exactly 1 damage after 3.0 continuous seconds inside any Poison Field, then 1 damage every 3.0 seconds while remaining exposed; entering while Verdant dust is equipped causes no damage and does not advance the vulnerable exposure timer. Switching from Verdant to any non-Verdant dust while still inside deals 1 damage immediately and starts a fresh 3.0-second cadence; equipping Verdant or leaving all Poison Fields cancels/resets the cadence, and overlapping Poison Fields must not stack or multiply damage. Use the authoritative equipped dust type and existing player-damage transaction, respecting death, pause, room-load, respawn, and transition lifecycle without ordinary contact invulnerability accidentally suppressing scheduled ticks. Visuals must be render-only purple-tinted cloud wisps at roughly 10% peak opacity, with soft/noisy boundaries, no strong rectangular silhouette, slow amorphous drift, and independently fading/pulsing patches; constrain coverage to the authored field while feathering its edges, keep simulation deterministic, and quality-gate/cap particle or noise work. Add complete editor palette/placement/selection/resize/undo/redo/copy-paste/save-load/compact-schema support and a readable but subtle editor preview. Likely files/subsystems: field/editor element types and palette/overlay drawers, room JSON/schema serializer/hydrator, runtime hazard loading/tick/damage state, selected-dust helpers, hazard rendering, and focused tests. Add deterministic tests for the initial 3-second grace, recurring ticks, timestep subdivision, Verdant immunity, instant damage when switching inside the field and the subsequent cadence, switching back to Verdant, re-entry reset, overlap non-stacking, lifecycle cleanup, save round trips, and renderer opacity/bounds; manually verify the clouds remain barely visible and shape-soft at every quality level, then run targeted tests, `npm run build`, `npm run lint`, and `npm test`; increment `BUILD_NUMBER` exactly once for code changes.

- [x] Give equipped **Verdant Dust** a high-speed grounded mobility identity with a deliberate grapple tradeoff and strong plant-themed feedback. (Completed in BUILD 596: added `src/sim/clusters/verdantMobility.ts` with the single authoritative `isVerdantDustEquipped(world)` predicate plus `VERDANT_GROUND_SPEED_MULTIPLIER` (2.0) and `VERDANT_JUMP_LAUNCH_MULTIPLIER` (1.5) constants, used everywhere instead of scattered `selectedDustKind === Nature` checks. Grapple: `fireGrapple` (`src/sim/clusters/grapple.ts`) early-returns while Verdant is equipped (input inert); `setSelectedDustKind` (`src/sim/weaves/selectedDust.ts`) calls the existing authoritative `releaseGrapple` (`grappleShared.ts`) when Verdant becomes equipped mid-grapple — `releaseGrapple` never touches `hasGrappleChargeFlag`, so charge state is never consumed/corrupted, and switching away restores ordinary grapple behavior immediately since the guard is keyed live off `selectedDustKind`. Movement: `playerHorizontalMovement.ts` multiplies only `baseRunSpeed`/`baseGroundAccel` (grounded branch) by the Verdant multiplier on top of the existing (possibly debug-overridden) authoritative constants — air accel, deceleration, ice, wall-jump-force-time, and the grapple-active early-return path are untouched. Skid-jump vertical launch (`playerVerticalMovement.ts` direct/coyote path and `movement.ts` landing-buffered path, both funneling through the shared `computeSkidJumpSpeedWorld`) and wall-jump horizontal+vertical launch (`playerWallJump.ts::attemptWallJump`) are multiplied by 1.5x after tier/debug-override resolution, preserving tiers/progression, direction, and ordinary (non-skid) jumps. Afterimage trail: `src/render/clusters/verdantAfterimageTrail.ts` (`VerdantAfterimageTrail`, render-only, bounded to 15 entries) samples the player's real resolved `getPlayerSprite(...)` image (pose/facing already baked in, no silhouette approximation) once per ~3 world-unit interpolated-position delta (distance-based, not per-frame) via a new `updateVerdantAfterimageTrailFrame` in `src/screens/gamePlayerCloakUpdate.ts`, called every render frame alongside `updatePlayerCloaks`; renders via a green canvas filter with age-scaled brightness/alpha, drawn behind the player sprite in `gameRender.ts`. Flowers: deterministic per-pixel bloom decision lives in sim (`src/sim/clusters/verdantFlowerSpawn.ts::updateVerdantFlowerSpawn`, called once per tick after collision resolution in `movement.ts`) — tracks last-evaluated integer world pixel + a monotonic per-crossing sequence on new `ClusterState` fields, hashes `(pixel, sequence)` for a deterministic 1% roll per newly crossed pixel (all pixels crossed in one tick are each evaluated exactly once), and publishes bounded events via new `WorldState.verdantFlowerEventCount/XWorld/YWorld` fields consumed immediately after every tick by a render-only bounded pool (`src/render/verdantFlowerTrail.ts`) with bloom/hold/wilt animation. Both trails reset in `loadRoom()` (`gameScreen.ts`) and the resident-activation path (`gameLoadRoomPhases.ts`), matching existing `momentumTrail`/`dustContainerPickupEffect` lifecycle wiring; natural fade-out (not an explicit clear) is used for the dust-unequip case per the item's "pick whichever is cleaner" allowance. Added `src/tests/verdantMobility.test.ts` (33 tests) covering grapple suppression/mid-grapple release/charge preservation/restoration-after-switch, exact 2x grounded speed/accel with air-accel/grapple-active isolation and timestep-subdivision stability, exact 1.5x skid-jump launch with ordinary-jump isolation, deterministic flower-spawn pixel-crossing semantics (multi-pixel-per-tick, timestep-subdivision equivalence, no-roll-while-stationary, independent re-crossing rolls), and afterimage-trail bounding/dedup/ordering/reset. Validated: full suite 3040/3040 tests pass, `npm run lint` clean (only one pre-existing unrelated `no-explicit-any` error in `roomLoadingIntegration.test.ts`, not touched by this change), targeted `tsc --noEmit` scoped to every changed file compiles clean. `npm run build` could not be validated end-to-end this session — the working tree had unrelated, already-in-progress uncommitted changes to `src/main.ts`, `src/levels/roomFileLoader.ts`, `src/screens/zoneResidentLoader.ts`, `src/editor/editorController.ts`, `src/levels/roomFileCacheState.ts`, `src/screens/gameTransitions.ts`, and `electron/campaignExport.cjs` (none touched by this task) that fail strict `tsc` with unused-import and a missing `getActiveManifest` export; see `nextSteps.md`. Manual in-browser verification of the green afterimage visual, flower bloom feel, and speed/jump feel was not performed — no DOM/canvas harness in this environment.)

## Completed / superseded references

The following are documented as complete in existing planning notes. Keep them here only as routing references for future agents.

- [x] Room schema v3 compressed 1x1 wall storage. See `nextSteps.md`.
- [x] Water/lava, ambient blocker, and background block compression additions. See `nextSteps.md`.
- [x] Complete boundary walls plus independent trigger strips. See `nextSteps.md` and `src/levels/roomBoundaryWalls.ts`.
- [x] Baked runtime wall templates persisted and preferred at runtime. See `nextSteps.md`.
- [x] Render chunk prewarming documented. See `docs/render-chunk-prewarming.md`.
- [x] Per-transition profiler and render-state key memoization. See `nextSteps.md`.

## Validation commands

```bash
npm run build
npm run lint
npm test
```

For transition/performance work, include browser profiler output or summarized `__dwTransitionStats()` results when possible.
