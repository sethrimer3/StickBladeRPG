# Hand-Drawn Map Prototype — Greybox Notes

Companion notes for `hand_drawn_stickblade_map.sbcampaign.json`, generated from
the April 26, 2026 hand-drawn "Dust, Wurm" map sketch. This is a greybox: macro
layout, room graph, gates, and mechanics placement — not final art or tuning.

Load from **Main Menu → Custom Campaigns** (auto-discovered from this folder).

## Graph deviations from the sketch spec

- `room_lower_water_vines_gate` was referenced once in the spec's connection
  list but is not in the 30-room list — dropped. `room_ice_water_vines`
  connects left (ice entry) and right (light/dark shaft) only.
- Redundant edges collapsed to a single authored path:
  - `speed_check_water → ice_zone_entry` routes through `speed_check_exit`.
  - `dark_zone_lower_entry → golden_dust_region` routes through `dark_zone_main`.
  - `overgrown_upper → magma_entry` dropped (magma is reached via the shaft).
- `golden_dust_region ↔ grapple_vault_region` is a vertical connection,
  matching the suggested world-map coordinates.
- `room_lower_caves_swing` has two left doors: an elevated ledge to
  `lower_caves_reset` and a floor-level door to `falling_block_puzzle`.

## Engine fix required (included in this change)

`src/screens/gameStartRoom.ts` — the failsafe-editor heuristic used to require
a room literally named `lobby` in the registry. Packed custom campaigns replace
the registry, so playing ANY custom campaign force-opened the editor visual
map, which consumed every frame and left the zone loader stuck at 0/N on the
loading screen. The heuristic now only applies when no explicit start room was
requested.

## Placeholders / TODO (no engine support found at generation time)

| Sketch note | Greybox stand-in |
| --- | --- |
| Room locks during enemy/boss fights | Open arenas with placeholder encounters (`room_enemy_lock_room`, `room_boss_lock_arena`, `room_boss_ice`) |
| "Require Ice to cross" (sunken ship) | Soft gate — ship hull platforms allow crossing; wire freeze gating later |
| "Vines to burn" | Breakable-block curtains (`room_ice_water_vines`); a Fire dust jar is placed one room earlier |
| Teleporter / fast travel | Lit white-marble dais in `room_teleporter_room` |
| Dark-zone enemy (shadow moves in light / white shadow in dark) | Flying eyes carrying Shadow dust |
| Weak save point (no loadout change) | Normal save tombs |
| Room renaming on game knowledge ("Mysterious Chamber → Seal Chamber") | Not implemented |
| "Golden Door", "Sec." marker | Crumble-wall secret alcove in `room_grapple_vault_region`; Golden Door unmodelled |
| Element progression (Gold→Plant, Water→Fire, Ice→Light, Void→?) | Not hard-coded; dust jars seeded per biome (Void, Fire, Ice, Physical) |
| Speed check as hard momentum gate | Runway with momentum gaps + bounce-pad recovery so it is passable pre-tuning; guide-dust path shows the line |

## Progression seeds placed

- `room_boss_ice` reward: Ice dust jar + `storm` weave tomb (stands in for the ice ability).
- Save tombs: desert start, speed-check exit, boss-ice approach, central hub,
  dark-zone entry, overgrown shaft, teleporter room.
- Dust containers: speed-check exit, boss-ice approach, boss-lock arena,
  golden dust region, teleporter room.

## Regeneration

Generated by `scripts/generate-hand-drawn-campaign.mjs` (run from the repo
root: `node scripts/generate-hand-drawn-campaign.mjs`). Deterministic geometry:
3-block floors with door gaps, ledges for elevated doors, one-way platform
stacks under every up-transition, greedy rect compression. Regenerating
overwrites hand edits — once you start refining in the in-game editor (whose
Export Campaign JSON round-trips this file), treat the JSON as the source of
truth and retire the script.
